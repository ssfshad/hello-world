-- A worked example per concept: code the learner wrote, and the output they
-- got from their own compiler (the app has no compiler).
ALTER TABLE concepts ADD COLUMN example_code TEXT;
ALTER TABLE concepts ADD COLUMN example_output TEXT;
